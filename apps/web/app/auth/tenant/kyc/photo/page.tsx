import { PageProps } from "_types";
import { KycSelfPage } from "~/tenant/auth/self-page";

const KycPhotoPage = async (props: PageProps) => {
  return <KycSelfPage />;
};

export default KycPhotoPage;
